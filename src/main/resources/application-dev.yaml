spring:
  datasource:
    url: jdbc:mysql://localhost:3308/mypassport?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
  mail:
    host: smtp.example.com
    port: 587
    username: dev-user@example.com
    password: dev-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


