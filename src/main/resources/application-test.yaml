spring:
  datasource:
    # Assuming a separate test database server
    url: jdbc:mysql://test-db-server:3306/mypassport_test?useSSL=false&serverTimezone=UTC
    username: test_user
    password: test_password
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: test-redis-server
      port: 6379
  mail:
    host: smtp.test-mail.com
    port: 587
    username: test-mailer@example.com
    password: test-password

server:
  port: 8080 # Standard port for test/stage often varies or stays 8080
